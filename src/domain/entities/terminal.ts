export interface TerminalConfig {
  id: string;
  projectId: string;
  connectionId: string;
  slot: number;
  command: string;
  label: string;
  order: number;
  createdAt: string;
  updatedAt: string;
}
